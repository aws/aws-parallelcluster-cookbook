---  # aarch64 packages (Basically only compiler)
packages:
  gcc:
    compiler: [gcc]
    require:
      - one_of: ["gcc@12 +binutils ^binutils@2.37 target=aarch64"]
  all:
    compiler: [gcc, clang]
    permissions:
      read: world
      write: user
