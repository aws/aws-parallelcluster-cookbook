<%# Logrotate file for slurm_resume log %>
/var/log/parallelcluster/slurm_resume.log {
    rotate 1
    size 50M
    missingok
    notifempty
    postrotate
        pkill --signal=SIGUSR2 slurmctld
        echo 0
    endscript
    su pcluster-admin pcluster-admin
    nodateext
}
